# 复制此文件为 .env 并按需修改
# cp .env.example .env

# LLM CLI 工具调用命令（含所有必要参数）
# 支持两种模式，脚本会自动适配：
#   1. Claude: claude --dangerously-skip-permissions
#   2. Cursor: cursor-agent -p --force
#
# 示例：
#   claude --dangerously-skip-permissions
#   cursor-agent -p --force
CLI_TOOL="claude --dangerously-skip-permissions"
